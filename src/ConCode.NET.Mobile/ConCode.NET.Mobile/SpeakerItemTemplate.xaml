<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="ConCode.NET.Mobile.SpeakerItemTemplate">
	<ContentView.Content>
		<StackLayout Spacing="0" Padding="0">
			<Grid Style="{DynamicResource CardStyle}">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="85"/>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="5"/>
				</Grid.ColumnDefinitions>
				<StackLayout Grid.Column="0" Spacing="0" HorizontalOptions="Center" VerticalOptions="Center">
				<Image MinimumHeightRequest="30" Source="{Binding Photo}"/>
					
				</StackLayout>
				<StackLayout Grid.Column="1" Spacing="0" Padding="0,5">
					<Label Text="{Binding FullName}" Style="{DynamicResource NameStyle}"/>
					<Label Text="{Binding TagLine}" Style="{DynamicResource DepartmentStyle}"/>
				</StackLayout>
			</Grid>
		</StackLayout>
	</ContentView.Content>
</ContentView>
